(
    meta_format_version: "1.0",
    asset: Load(
        loader: "bevy_render::texture::image_loader::ImageLoader",
        settings: (
            format: FromExtension,
            // Should be false, but will require re-generating the asset.
            is_srgb: false,
            sampler: Default,
        ),
    ),
)
